import { TemperatureConverter } from "@/tools";
import { moduleConfig } from "@/components/ToolModule/config";

export const metadata = {
  title: moduleConfig["TemperatureConverter"].title,
  description: moduleConfig["TemperatureConverter"].description,
};

export default TemperatureConverter;
