import { RandomGenerator } from "@/tools";
import { moduleConfig } from "@/components/ToolModule/config";

export const metadata = {
  title: moduleConfig["RandomGenerator"].title,
  description: moduleConfig["RandomGenerator"].description,
};

export default RandomGenerator;
