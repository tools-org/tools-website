import { WriteOnline } from "@/tools";
import { moduleConfig } from "@/components/ToolModule/config";

export const metadata = {
  title: moduleConfig["WriteOnline"].title,
  description: moduleConfig["WriteOnline"].description,
};

export default WriteOnline;
