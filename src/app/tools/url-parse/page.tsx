import { UrlParse } from "@/tools";
import { moduleConfig } from "@/components/ToolModule/config";

export const metadata = {
  title: moduleConfig["UrlParse"].title,
  description: moduleConfig["UrlParse"].description,
};

export default UrlParse;
