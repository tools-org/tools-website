import { TextStatistics } from "@/tools";
import { moduleConfig } from "@/components/ToolModule/config";

export const metadata = {
  title: moduleConfig["TextStatistics"].title,
  description: moduleConfig["TextStatistics"].description,
};

export default TextStatistics;
