import { Timer } from "@/tools";
import { moduleConfig } from "@/components/ToolModule/config";

export const metadata = {
  title: moduleConfig["Timer"].title,
  description: moduleConfig["Timer"].description,
};

export default Timer;
