import { ImageToText } from "@/tools";
import { moduleConfig } from "@/components/ToolModule/config";

export const metadata = {
  title: moduleConfig["ImageToText"].title,
  description: moduleConfig["ImageToText"].description,
};

export default ImageToText;
